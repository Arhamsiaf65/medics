import { Server } from "socket.io";
import { Server as HTTPServer } from "http";
export declare const initializeSocketServer: (server: HTTPServer) => Server;
export declare const getIO: () => Server;
//# sourceMappingURL=connectSocket.d.ts.map